import { postDate } from '../../../helpers/post-date';
import { module, test } from 'qunit';

module('Unit | Helper | post date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = postDate(42);
  assert.ok(result);
});
