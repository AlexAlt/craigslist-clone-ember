import { isCommunity } from '../../../helpers/is-community';
import { module, test } from 'qunit';

module('Unit | Helper | is community');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = isCommunity(42);
  assert.ok(result);
});
