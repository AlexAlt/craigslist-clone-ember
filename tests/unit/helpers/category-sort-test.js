import { categorySort } from '../../../helpers/category-sort';
import { module, test } from 'qunit';

module('Unit | Helper | category sort');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = categorySort(42);
  assert.ok(result);
});
